import React from "react";
import { SearchContainer } from "../utils/ExportComponents";

const SearchPage = () => {
  return (
    <div className="h-screen w-screen bg-black text-black">
      <SearchContainer />
    </div>
  );
};

export default SearchPage;
